

mybatis:
    mapper-locations: classpath:mapper/**/*.xml
    configuration:
        map-underscore-to-camel-case: true
    type-aliases-package: com.moneybug.bug..vo


spring:
    datasource:
        url: jdbc:mysql://192.168.219.100:3306/moneybug
        username: hum
        password: 1234
        driver-class-name: com.mysql.cj.jdbc.Driver
    mustache:
      servlet:
          expose-request-attributes: true

    mail:
      host: smtp.gmail.com
      port: 587
      username: dnrgks142@gmail.com
      password: owwibixtmpwevrwh
      properties:
          mail:
              smtp:
                  auth: true
                  timeout: 5000
                  connection-timeout: 5000
                  write timeout: 5000
                  starttls:
                      enable: true
                      required: true

auth:
  code-expiration-millis: 1800000  # 30 * 60 * 1000 == 30분

#session 소멸 시간 설정.
server:
    port: 8082
    servlet:
        session:
            timeout: 90m


logging:
    level:
        com.moneybug.bug: DEBUG
        org.mybatis: DEBUG


